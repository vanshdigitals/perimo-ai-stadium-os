# --- Runtime ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
# Gemini SDK. Imported lazily and only used when GEMINI_API_KEY is set,
# so the app (and the whole test-suite) runs fully offline without it.
google-generativeai>=0.5,<1.0

# --- Testing / tooling ---
httpx>=0.27,<1.0          # used by starlette.testclient.TestClient
pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
ruff>=0.6,<1.0            # linter/formatter (config in pyproject.toml)
mypy>=1.10,<2.0           # static type checker
